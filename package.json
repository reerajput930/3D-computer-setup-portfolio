{
  "dependencies": {
    "three": "^0.183.2"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
    "scripts": {
    "build": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
